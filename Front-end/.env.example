VITE_API_URL = http://localhost:5000/api

# Google Identity Services client id (public — it ships inside the bundle).
# Leave empty to hide the "Continue with Google" button entirely.
# The same id must be set as GOOGLE_CLIENT_ID on the API, which verifies the token.
VITE_GOOGLE_CLIENT_ID =
