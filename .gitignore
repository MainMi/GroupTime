# Dependencies & build output
node_modules/
build/
coverage/

# Secrets — keep .env.example / .env.test (no secrets) tracked
.env
.env.local
.env.*.local

# Local notes, experiments and anything not part of the project
_local/

# Telegram bot — kept out of version control for now (self-contained service,
# has its own package.json and reads TELEGRAM_BOT_TOKEN from Back-end/.env)
/telegram-bot/

# Docs and Claude Code working files — the project docs are the exceptions
*.md
!README.md
!README.uk.md
!DEPLOY.md
CLAUDE.md
.claude/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*